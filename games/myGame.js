function setup() {
  const canvas = createCanvas(400, 400);
  canvas.parent('gameContainer'); // להכניס ידנית לתוך הקונטיינר
  background(30);
  textAlign(CENTER, CENTER);
  textSize(24);
  fill(255);
  text("ברוך הבא למשחק הדמו!", width / 2, height / 2);
}

function draw() {
  // לא קורה כלום - רק הדגמה
}
